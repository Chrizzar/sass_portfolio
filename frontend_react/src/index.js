import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';
import './index.css';

// Connect to the App component straight to the div with the id of root
ReactDOM.render(<App />, document.getElementById('root'));